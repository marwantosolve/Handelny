# Base URL for the Handelny FastAPI backend.
# Copy this file to .env.local and adjust if your API runs elsewhere.
# Defaults to http://localhost:8000 when unset.
NEXT_PUBLIC_API_URL=http://localhost:8000
